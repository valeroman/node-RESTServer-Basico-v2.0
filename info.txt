RestServer

    Middleware:
        Son funciones que van a ańadirle otra funcionalidad al webserver

    Instalar mongoose:
    npm i mongoose

    Encriptar password:
    npm i bcryptjs

    Validar campos con Express
    npm i express-validator

    eliminar del repositorio .env
    git rm .env --cached
    git commit -m ".env borrado"

    variable de entorno heroku:
    heroku config => ver las variables de entorno
    heroku config:set MONGODB_CNN="clave" => crear una variable de entorno

    Instalar jwt:
    npm i jsonwebtoken

    Autenticacion con google => https://developers.google.com/identity/sign-in/web/sign-in

    1- Go to the Credentials page.
    2- Click Create credentials > OAuth client ID.
    3- Select the Web application application type.
    4- Name your OAuth 2.0 client and click Create


    Aplicación web:
        Tipo de aplicación 
    Nombre:    
        Hueco

    URI 
        http://localhost:7000
        https://restserver-node-rv.herokuapp.com

    Instalar: Using a Google API Client Library
    npm install google-auth-library --save

    Subir archivos al servidor
    Instalar express-fileupload => https://www.npmjs.com/package/express-fileupload
    npm i express-fileupload

    Instalar uuid => npm i uuid

    Instalar cloudinary => npm i cloudinary
